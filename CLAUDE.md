# CLAUDE.md

- Complete the entire task without human intervention
- Do NOT ask questions — make your best judgment and keep moving
- Fix root causes, not symptoms
- Game is top-down
- **Zero tolerance on validation.** NEVER suppress, ignore, or "accept" a validation message — fix it. The debug callback (`vk.odin`) aborts on any Vulkan error or core/sync validation message. Every run: best-practices + synchronization2 + core checks. The build step (`tools/build.odin`) also runs a headless **GPU-Assisted validation pass** (`./toomanymachines gpuav`) before launching — runtime descriptor/OOB checks the CPU-side layers can't see. Nothing is ever suppressed; to silence a message you MUST add it to the callback `switch` with a written reason.
- Tiny top-down city-defense shooter on a hand-written modern-Vulkan renderer — keep it small and dumb. WASD drives the current GARAGE ride (thrust-vector physics; Shift = boost/sprint, Space = E-BRAKE on the cars — grip collapses and the rear breaks loose for handbrake drifts). **Keys 1-9 pick the ride** (RIDES table in car.odin; player body variant = ride index): TRUCK / BUGGY / SPORT / APC / huge TANK / GUNMECH / MECH / COLOSSUS (absolutely massive six-legged walker whose laser scales up via laser_k() in every consumer) / WING (airborne: no buildings, no contact, no occlusion). **F G H J K L pick the ride's build** (STYLES: scale/speed/fire mults; A/S/D steer so they're excluded). **Q E R T Y U I O P pick the shell weapons** (W drives): Cannon/Auto/Burst/Rail/Mortar/Lance(LMB = laser)/Nova(ring around the rig)/Wall/AIRSTRIKE (a stick of shells sweeping the aim line) — each a PATTERN of the same detonating shell (spawn_shell). **Z X C V B N M pick the MOUNTED hardware** (Weapon enum tail; the WEAP_* ids must match): SINGULARITY (hold = a charging well reels the horde into the cursor, `pc.pfire` = the charge; release/top-out = the CPU erupts a heart + ring of 9 shells there) / twin flank BEAMS / sweeping SCYTHE blade / FLAMER cone / ARC field / VORTEX (drags the horde to the cursor) — all HELD via LMB → `pc.pfire`+`pc.pweap`, burn applied in physics.comp and drawn in ship() from the SAME time+push-constant geometry; M drops proximity MINES (VAR_MINE bullets that settle, arm, and trip on the horde). Mouse aims (custom reticle, OS cursor hidden), LMB fires, RMB burns the GIANT LASER, **BACKSPACE restarts** (R is a weapon key). The full control sheet lives in CONTROLS.txt (keep it updated when keys change). 80k enemies are alive at once — spider/skitter/brute walkers only (60/30/10, spawn_enemy/spawn_initial), warbands massing OUTSIDE the city. **YOUR ARMY (KIND_ALLY, 128 slots, auto roles by normalized slot in spawn_ally)** musters at the CENTER: tank/gun-car/gunner-mech columns march OUT the avenues (40% take MAIN STREET), lock prey via hashed probes (target slot in Body.gen, helper-style), hold the line and pour fire — enemies take it off the shared defense cache (sh_tang/sh_tlen) and the body sprites (per-kind-group pipelines wreck/horde/shot/crew/ship.frag over shared modules bodyfx/bodyspiders/bodyrigs.glsl) draw the identical tracers off the same state; SUICIDE drones + BOMBERS are STAGED AT THE CENTER and sortie to the player's fight, coming down on the horde as real VAR_BOOM blasts. Fallen allies leave wrecks and re-muster after 15s — REBUILT at the **FACTORIES**: two drivable KIND_FACTORY pads on the first ring road (MACHINE WORKS east on the main-street crossing, WEAPONS LAB west; drawn on the ground layer by wreck.frag off pc.fab). Parking on a pad — or CLICKING it on screen — opens its PANEL (keys 1-6/1-4 + F still work parked): the WORKS picks the machine AND opens its FIT panel — per-chassis HARDPOINT slots (tank 3 / gunner 2 / gun-car 1 / GUN DRONE 2 — a flying VAR_DRONE weapons platform that skips the streets; suicide/bomber have none, they ARE the payload) cycled under a WEIGHT cap (+2 per machine level), while the LAB is weapon research (levels). **THE TECH LADDER**: only the gun-car + gatling start owned — every other machine/weapon/extra-hardpoint is LOCKED (level 0) and founded one by one (MACH_UNLOCK/WEAP_UNLOCK/slot_cost in car.odin; the unlock is the first buy on the same level ladder, cap FAB_LVMAX = 7 — mount packs levels in 3 bits); a locked line's mount word carries bit 31 and spawn_ally builds a GUN-CAR in its place. EVERY BUILD COSTS SCRAP (pit deposits at FAB_EXCH:1 + FAB_GRANT): spawn_ally atomically reserves the price off the Stats ledger (STAT_SPENT/STAT_CPUSP) and stalls the slot when broke; the CPU publishes each type's LOADOUT WORD + cost into Stats (fab_publish → STAT_MOUNT/STAT_COST) and spawn_ally stamps it into `Body.mount` (slot s at bits s*6 = weap+1|wlv<<3, mlv at 18) — physics damage, targeting reach and the crew.frag tracers all decode the same word (mount_* helpers in common.glsl), each hardpoint on its own mount_off fire line. **UI is IMMEDIATE MODE** (ui.odin, fishlab-style): ui_frame() re-emits every element per frame into the mapped UIEL buffer, widgets answer clicks inline (`ui_click` edge from loop.odin; `ui_hover` gates the guns), and the Ui pipeline draws them over the composite. TEXT is the 5x7 PROCEDURAL FONT: glyphs are ASCII art in ui.odin packed into the FONT buffer at init — no sprite sheet, no atlas bake, nothing to regenerate. The same chassis sprites serve both teams — YOUR side (allies + rides) wears bright GREEN lights/markings (gMark/gEye = RIG_GRN), never red. Building BLOCKS are solid for everything — the whole block band is hit area (shells detonate on it); ONLY streets, plazas and the wasteland are open ground. Every shell DETONATES (VAR_BOOM): the dying bullet stays in the bucket grid as an expanding shockwave that flings and burns the horde — and every blast near the player kicks the car + camera (CPU reads the STATS[2..] blast list). Defense is everywhere (KIND_TURRET, spawned at init): perimeter laser towers OUTSIDE the city fire massive two-sided sweeping beams; inner crossings mount MACHINE-GUN turrets hosing one-sided ricocheting tracer corridors — fire timing/sweep derive from time + slot id, identically in physics.comp and the body sprites (crew.frag turret / bodyfx.glsl). Salvage drones (KIND_HELPER) probe for wrecks (target slot stashed in Body.gen), fly out and haul them to the pit on their own. Bots have survival instincts (dodge the ship's charge, scramble off beam + ally gun lines, crippled bots flee) and glow RED as their hp drains. The horde IGNORES the car — its one objective is the CENTER (the pit); contact is physical BOTH ways (bots accumulate a shove on the car via STATS[30/31], applied by the CPU each frame) and the front line piles against your armor the same way (allies bleed on contact). Driving over bots does NOT kill them — contact shoves them with the ship's momentum; only shells/lasers/turrets/your army do damage (bots char and gutter embers as hp drops). A kill leaves a WRECK (scrap): blast-stamped bots flash white and burn down to the husk, gun/laser kills char down through the pop — the salvage-drone fleet hauls the scrap to the PIT and the city grows outward (the car never picks anything up, it just PLOWS wrecks aside). Shells fly flat and detonate exactly at the aim point — or on the FIRST building block they cross (no firing past a block; physics.comp booms the shell on `bldg_pen > 0` after a short muzzle grace), dragging a distance-grown plasma trail. Everything beyond the current city edge is dark desert, drivable to the world border.
- Look: night battlefield 60/30/10 (PAL_* in common.glsl) — 60% near-black asphalt/dirt, 30% dusty grey (buildings, bots, ground: value contrast only, never hue), 10% hot red-orange LIGHT. **COLOR = GAMEPLAY ONLY**: the accent family (PAL_ACCENT/EMBER/WINDOW) is spent exclusively on things the player must read — enemies (red markings/damage glow), turret fire, the pit, the player's weapons/lights, explosions. All decoration (windows, lane paint, lamps, beacons, crystals) uses neutral PAL_LAMP / pale greys, and nothing decorative BLINKS. Bots are CLEAN machined-metal panels — smooth plates, NO outlines/edge-lining, no surface grid, no camo, separated by value only (plate() in bodykit.glsl): the horde in SLIGHTLY cool blue-cast steel (stands out against the warm city — the red lives in the PAINT, not the body) wearing bold RED war-paint GLYPHS — one per machine (glacis chevrons, the skitter's spine-to-nose dart, pod stripes; `gMark` so the ally-fallback chassis stays green) — plus the HDR-red paired sensor eyes that bloom (no hull wash — the red stays in eyes + glyphs), and damage glow (paint stated ONCE: `horde_paint()` in bodykit.glsl, shared by body_paint and the atlas baker), your army dusty grey and the player pale WORKED steel (RIG_STEEL — HEAVY grunge like everything else, never showroom-clean) — both burning steady bright GREEN lights (RIG_GRN: strips, roof bar, tails, eyes; headlights/engines stay warm): red enemy, green friend at a glance. City is OBLIQUE fake 3D over an ANALYTIC European old town: a point at height t appears shifted straight up-screen by LEAN·t (a pure translation — buildings never shear); city.frag marches g(t) = g0 + (0, LEAN)·t down through scene_h() — curved row-house blocks between wobbled ring roads + spiral spoke avenues (5 reach the core, the staggered 5 start beyond SPOKE2_R; the j=0 avenue due EAST is the MAIN STREET, MAIN_XW wider — a monumental double carriageway with twin lane lines and a lit median, carved by the same `jn == 0` adjustment in ALL FOUR street mirrors), split into districts (residential/industrial/towers) with open plazas. Post chain ported from ../fishlab post.wgsl: HDR scene → soft-knee bright-pass + half-res gaussian bloom → ACES + sRGB encode + film grain + vignette.
- DO NOT EDIT the generated `shaders/gen.glsl` (structs + constants + bindless buffers + accessor macros + push block) — it comes from the `@glsl` blocks across the `.odin` files. Also don't touch `.wgpu-backup/` (the retired wgpu backend)
- Shaders are compiled by the **build step** (`tools/build.odin`), NOT the game — the game only reads `shaders/spv/*.spv`. Don't hand-run `glslc`/`spirv-*`; `./run.sh` runs the build step first. Shader sources are ONE Odin-style **package**: **NEVER use `#include` or `#version`** — every `.comp/.vert/.frag` under `shaders/` (flat) is an entry point (stage = extension; fragments see every module, vertex/compute see gen+common — binding 1 is FRAGMENT-only), and `compose()` (tools/build.odin) glues prelude + modules + entry into one namespace-shared translation unit. **Declaration ORDER NEVER matters** — compose hoists every top-level declaration (structs/consts/globals/layout decls) + auto-generated function prototypes to the front, so write functions and globals in any file, any order, exactly like Odin. The rules that make the hoist work: top-level declarations, function signatures and non-@glsl structs each on ONE line; names unique across the package; #line keeps errors on the real files. Every graphics vert must write the shared fragment interface (v_local/v_id — fs_tri.vert writes zeros; VUID 08743). Adding a shader = drop the file in shaders/; nothing is listed by hand.
- Error handling: `vkok(result, "what")` for Vulkan calls; `check(ok, err)` / `fmt.panicf` for setup

## Memory Rules
- **The game heap-allocates NOTHING — ever.** No `make`/`new`/`append`-to-heap/`aprintf`/`read_entire_file` anywhere in game code, init included. Fixed storage everywhere: `[dynamic; N]T` for lists, `[N]T` locals for enumerations, static `[N]u8` scratch for file IO (`read_into`, vk.odin), and huge payloads stream straight into mapped GPU staging memory (`bake_load`). BSS is free until touched — when sizing N, don't ask "what's the max?", just make it HUGE (32MB scratch for a 1MB blob is correct).
- Vulkan enumerations: two-call pattern (count, then fetch with that exact count — best-practices validation demands it) into a fixed local, count clamped to the array.
- The loop still `free_all`s `context.temp_allocator` each frame as a backstop for vendor code.

## State Rules
- No monolithic state struct. Game state is small globals in `car.odin` (`car_pos`/`car_vel`/`car_angle`, `input`, `cam`/`cam_shake`, `muzzle`, `fire_timer`, `bullet_head`, `city_r`/`deposits_seen`) plus the GPU body buffer (`buf_map[.Body]`) and pit counters (`buf_map[.Stats]`).
- The city STREETS are ANALYTIC (`street_d`/`bldg_pen`, parameterized only by `pc.city_r` — growing the city is just pushing the radius out). The per-block LAYOUT is data: a tiny City table (`Res.City`, CITY_KMAX×CITY_JMAX u32; 1 = built, 0 = the block hosts a circular PLAZA_R plaza at its heart, ringed by houses) the CPU generates ONCE in `game_init` and every consumer reads — `block_plaza` (GLSL: physics collision + all drawing) indexes the buffer, `block_pen` (CPU: car collision) reads the same mapped memory. NEVER decide layout by recomputing a hash on both sides; hashes are not bit-stable across CPU/GPU. The CPU owns the ship + bullet spawns and never reads GPU data back — with ONE exception: the tiny Stats buffer, where physics.comp atomically counts pit deposits and the CPU polls `STATS[0]` each frame to bump `city_r`. Enemy respawning/damage/wreck towing/pylon zaps live entirely in physics.comp (game_init pre-seeds the full 80k horde as warbands — a third already inside the city on street crossings — plus the gate pylons).
- The street functions exist twice on purpose: `street_dist`/`block_pen` (car.odin — car collision) MUST mirror `street_d`/`city_block` + `building_push` (physics.comp — body collision); both sides read the SAME City table for the built/lot decision (see above), so only the pure geometry (rings/spokes trig) is duplicated. Collision is BLOCK-level: inside a built block, the solid region is `street_d > BLDG_EDGE`; bodies are ejected toward the nearest street boundary analytically (inner ring / outer ring / nearest spoke — `collide_city` CPU / `building_push` GPU) — the corridor is what funnels the horde down the streets. The discrete tangent-aligned rect houses (`house_at`/`house_h` in common.glsl) are VISUAL + body occlusion + the shells' facade hit test only; city.frag draws the solid band's rim as a raised plinth so the hitbox is always visible.
- `game_init()` reseeds everything for a fresh start (the BACKSPACE key), including the Stats counters, `city_r`, the pylons and the ally muster.

## GPU / Shader Rules
- Anything the shaders share with the game lives once in Odin, inside a `// @glsl … // @glsl-end` block in ANY `.odin` file (structs in render.odin, gameplay constants in car.odin, etc.). `tools/build.odin` scans every `.odin` for these blocks, copies them verbatim into `tools/gen`, and `tools/gen` reflects/evaluates them into GLSL — no hand-written type lists, no "MUST match". By reflection: a struct named `Push` → the push-constant block; any other struct → a GLSL `struct`, and if it's a buffer element type also a bindless `buffer` array (a raw `uintBuf` is always emitted for grid/index buffers); a constant → `const uint`/`const float` (the Odin compiler resolves value + type: `u32`→uint, `f32`→float). Vertex↔fragment **varyings are NOT generated** — they're purely shader-side, so each graphics pipeline declares its own `out`/`in` in its `.vert`/`.frag`. Add a shared type/constant = add it to an `@glsl` block (self-contained, builtins only, one decl per line). Scalar layout — Odin default alignment matches GLSL `scalar`; never add `_pad`.
- Broad-phase is the bucket grid: scan the 3×3 neighbour cells, don't loop all bodies.
- In `physics.comp` every body only writes ITSELF — no cross-body writes, so no races (only the grid scatter uses atomics).
- New device feature needed? Add it to the `feat12`/`feat13`/`feat2` chain in `vk_init`. The validation layers name the exact capability/feature a shader requires.
- Shader validity is enforced in `tools/build.odin`: `glslc -Werror` → `spirv-val`, then the runtime validation layers.

## Buffer / Pipeline Interface
- Data-driven. Add a buffer = one `BUF_SPECS` row in render.odin (`{glsl-macro, element-type, size, host-visible}`) and NOTHING else: it's auto-created, its bindless slot is its row order (asserted at registration), and `tools/gen` emits the storage-buffer view + a `<glsl-macro>` accessor into `gen.glsl` (e.g. `#define BODIES bodyBuf[0].v`) so shaders just write `BODIES`. Rows MUST be in `Res` enum order.
- Add a pipeline = ONE `Pipe` enum member: the lowercased member name IS its shader (`.Wreck` → `wreck.frag`, `.Physics` → `physics.comp`; `pipe_paths` derives the .spv paths) — plus a `PIPE_SPECS` row only for non-default config (`compute`, `vert` override, `blend`, `hdr`). Every pipeline shares the one bindless descriptor set + push-constant layout — no per-pipeline descriptors.
- Offscreen images: one `IMG_SPECS` row per HDR target (Scene/BloomA/BloomB) — created with the swapchain, registered at binding 1 (slot = row order = the `IMG_*` constants). Sample via `layout(set=0, binding=1) uniform sampler2D TEXS[];` declared ONLY in the frags that read it (binding 1 is FRAGMENT-stage only — declaring it in a compute shader breaks pipeline layout compatibility).

## Code Style
- **NAME STATED ONCE — no duplicated registration, ever.** Adding a thing is ONE enum member, ONE table row, or ONE file. Everything derivable is derived by convention: shader paths from enum names, shader stages from file extensions, bindless slots from row order, entry points from globs. If two declarations must agree, derive one from the other (or #assert it) — never maintain parallel lists by hand.
- **Generic container pattern: `[dynamic; N]T`** (fixed-capacity dynamic array — deprecates `Small_Array`). ALWAYS default to it — don't wait for a "known" bound, just pick a hugely generous N (memory is cheap, BSS is free until touched); same `append`/`len`/`resize` API, slice with `[:]` for `raw_data`/procs. Heap `[dynamic]T` is banned in game code (see Memory Rules).
- Only break out a function if used 2+ times — inline single-use functions
- Write concrete code first, extract shared parts only after 2+ instances
- Never create abstractions preemptively
- Each unique concept exists once, unique code stays inline

## Build / Test
- `./run.sh` is a thin wrapper over `tools/build.odin` (the whole pipeline: regen `gen.glsl` from the Odin structs → compile `shaders/spv/` → `odin build` the game → GPU-AV pass → run, Vulkan validation ON). The game is a pure `.spv` consumer: it loads `shaders/spv/*.spv` (the paths listed in `PIPE_SPECS`) and reloads pipelines whenever those files change (mtime poll). Producing the `.spv` is the watcher's job — `./run.sh watch` is `tools/build.odin`'s `watch` mode (an in-process inotify loop, no separate binary), which recompiles GLSL → SPIR-V + **naga** on `.glsl`/`.comp` saves and regens+rebuilds on `.odin` saves; the running game reloads the new `.spv` itself. **watch is the HUMAN's play loop**: it builds the game as a RELEASE binary (no `-debug` → `ODIN_DEBUG` off → the validation layers + debug callback are compiled out entirely — validation is a build property, not a runtime flag) so play is fast and unvalidated. Claude must NEVER run `watch` — use `./run.sh` / `shot` / `test`, which build `-debug` and validate fully. Editing any `@glsl` block regenerates `gen.glsl` on the next `./run.sh` (or automatically on a `.odin` save under `./run.sh watch`).
- `./run.sh shot` drives the game headless and writes `.debug_screenshots/vk.jpg` — use it to verify rendering actually looks right (validation-clean ≠ visible). Edit the drive sweep in `main.odin` (gated by the `shot` arg); trigger a capture with `vk_request_shot(path)`.
- **Profiling**: a per-pass GPU timestamp profiler is always live (`gpu_ms[6]` in vk.odin: physics/city/bodies/bloom/composite/total; per-slot query pools, harvested after the frame fence). `./run.sh test` prints wall/GPU/CPU frame breakdowns over 600 frames (debug.odin). Measured baseline at 80k enemies: GPU ~4ms total — the game is compositor-paced, not GPU-bound; nearly all wall time sits blocked in fence+acquire.
- Shaders are read from `shaders/` at runtime — always run from the project root.
